<!-- Thanks for contributing! Keep each PR to one logical change. -->

## What & why

<!-- What does this change do, and why? Link any related issue (e.g. Closes #N). -->

## Checklist

- [ ] `ruff check .` passes
- [ ] `ruff format --check .` passes
- [ ] `MPLBACKEND=Agg pytest` is green
- [ ] Conventional-commit style used (`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:`)
- [ ] **New concept only:** ships a small offline cache (`.npy` / `.npz`) + a `gallery/` sample, has a `README.md`, and is added to the `CONCEPTS` list in `tests/test_smoke.py`

## Sample renders

<!-- For visual changes, attach before/after screenshots or sample renders. -->
