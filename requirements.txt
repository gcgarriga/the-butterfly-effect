# Python 3.10+.
# Tested with: torch 2.12.1, transformers 5.12.1, numpy 2.5.0,
# matplotlib 3.11.0, pillow 12.2.0 (on Python 3.14).
# Floors install broadly; the transformers cap guards against a future major
# dropping the GPT-2 classes this project relies on.
torch>=2.12.1
transformers>=5.14.1,<6
numpy>=2.2.6
matplotlib>=3.10.9
pillow>=12.3.0
